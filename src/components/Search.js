// import { useState } from "react";

// const Search = () => {
//     const [searchText, setsearchvale] = useState("");
//     return (
//         <div className="search_sec">
//             <input type="text" className="search_input" value={searchText} onChange={ (e) => setsearchvale(e.target.value) } />
//             <button className="search-btn" onClick={()=> {
//                 const FilterList = listOfRes.filter((res) => res.info.name.includes(searchText));
//                 listOfRes(FilterList);
//             }}>Search</button>
//         </div>
//     );
// }

// export default Search;